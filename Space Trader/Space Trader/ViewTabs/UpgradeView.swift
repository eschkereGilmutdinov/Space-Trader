import SwiftUI

struct UpgradeView : View {
    var body: some View {
        VStack (spacing: 12) {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Улучшения")
                .font(.headline)
        }
        .padding()
    }
}
